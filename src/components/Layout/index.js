import NavigationBar from '../NavigationBar';
import './index.scss' ;

const Layout = () => {
    return (
        <div>
            <NavigationBar />
        </div>
        )
}

export default Layout;